import time
import random
import pygame


displayX = 800
displayY = 600
gameDisplay = pygame.display.set_mode([displayX, displayY])

def Display(x, y):
    return

loop = True
while loop:
    Display(5, 7)
